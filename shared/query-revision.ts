// Bump when derived query or pricing semantics change without changing the sync cut.
// This versions result DTOs only; raw entities, history and read leases keep their identity.
export const USAGE_QUERY_REVISION = "token-usd-v1";
