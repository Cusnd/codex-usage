# 私有统计同步

[返回总体统筹](README.md)

工作项：SYNC-01。状态以总览为准；具体复选框和完成证据在本文件维护。

2026-09-12 更新：下方已勾选事项及原完成记录对应此前 v2 生产验收。v3 功能已实现；全面验收的 10 项缺陷中，用户选中的[9 项已修复并定向复验](../design/multi-device-cloud-panel-fixes-2026-09-12.md)，S05 窄窗口菜单仍待处理，不能概括为整体验收全部完成。详见[本轮实施记录](../design/multi-device-cloud-panel-implementation-progress-2026-09-11.md)。[性能改进](cloud-sync-v3-performance.md)继续暂缓；100k 完整浏览器压力、远程平台矩阵、三台物理设备、24 小时运行和生产切换仍未完成，不能沿用 v2 已通过状态。此前候选包也不代表本轮修复已重新打包发布。

## 目标与当前基础

电脑离线后仍可查看最近成功同步的私有统计，并知道数据截至时间。

以下为 2026-09-09 本地源码和文档核查基线，实施时复核可能变化的行为；历史验收不代表本轮已验证。

| 方向 | 当前仓库证据 | 后续工作 |
| --- | --- | --- |
| 隐私与来源 | 缓存含任务标题、项目路径、用量元数据、账户快照，不存聊天正文；本地记录与账户快照分开 | “不是聊天正文”仍不等于可公开，远程和同步需要字段级数据边界 |

## 范围与依赖

依赖数据契约和 [COLLECT-01](collection.md)；复用 [MOBILE-01](mobile.md)，与远程查看统一数据质量语义。

共同目标、数据边界和阶段安排见[总体统筹](README.md)。

## 待办与验收

### SYNC-01：设计、实现及验收均纳入待办

- [x] 按 2026-09-11 用户确认的范围实现多设备私有统计，默认合并、支持单台/多台筛选；同一套 UI 覆盖总览、费用、趋势、项目、任务、轮次及 Agent。
- [x] 明确字段白名单：事件 UTC 时间、模型、Token 组成、质量状态、标识和 Agent 关系；本次明确授权保留原标题和完整项目路径，新绑定展示该范围，旧版设备需主动启用完整同步。
- [x] 禁止上传原始 session、聊天/工具正文、登录凭据和实例控制 token；不上传原始 SQLite 数据库。
- [x] 实现分块完整修订、持久化待传队列、确认水位、幂等重试、退避及完整性校验；旧数据可通过新修订替换。
- [x] Token 保持十进制字符串与 null；本地/云端查询共享逻辑，账户额度及每日历史单独同步，同账户跨设备识别与未知身份保持明确。
- [x] 使用现有 Worker/D1 和 GitHub OAuth，保留到主动删除，撤销与历史删除分开。云端可读取统计元数据，未宣称端到端加密；不自动升级付费计划，需按实际存储与请求量监控账户。
- [x] 本地后台出站同步、共享云端 UI 和合成 showcase 适配器分别运行，云端不访问本机 API。
- [x] 展示采集/接收时间、首次同步进度、设备状态、覆盖范围、来源与冲突提示。
- [x] 实现暂停、恢复、撤销、删除、断网及重启恢复；服务端与客户端测试覆盖账户切换、身份未知、迟到请求和用户隔离。
- [x] 完成真实历史全量接收、统计核对与停止采集后的历史查看，记录生产验收和回滚版本。

#### 验收标准

重复上传不重复计数，离线重连不丢数据，修正旧数据可以收敛；电脑关闭后另一台设备仍能读取最后成功快照并识别其陈旧性；传输载荷和云端存储经合成数据检查符合白名单；撤销、隔离和删除有效。

## 待决策问题

本次范围已由用户确认：云端读取全部已同步设备、本机入口只读本机；上传原标题与完整路径；统计持续保留到主动删除。物理手机、其他真实电脑采集端及 npm 发布不在本次已执行验收范围内。

## 完成记录

2026-09-11，`codex/cloud-quota`：多设备实现、自动化测试及生产验收完成。真实 GitHub 登录和新设备绑定成功；211/211 个会话完成首次同步；固定区间的 853 条记录、26 个会话、75 个轮次及所有摘要 Token 字段与本地一致。停止采集后云端保留历史并显示离线状态，随后恢复当前构建的采集服务并确认新回执及待传清零。共享 UI、CUA 桌面/窄屏及本机域名跳转均已验证。实际范围、限制和兼容回滚版本见 [部署与验收记录](../../cloud/DEPLOYMENT.md)；本次未进行 npm 发布、Git 合并或第二台真实电脑验收。
