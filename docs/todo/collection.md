# 持续采集

[返回总体统筹](README.md)

工作项：COLLECT-01。状态以总览为准；具体复选框和完成证据在本文件维护。

## 目标与当前基础

没有网页打开时，受管理服务仍按配置持续采集并报告新鲜度。

以下为 2026-09-09 本地源码和文档核查基线，实施时复核可能变化的行为；历史验收不代表本轮已验证。

| 方向 | 当前仓库证据 | 后续工作 |
| --- | --- | --- |
| 连续采集 | [前端](../../web/App.tsx) 用定时器触发刷新；服务启动触发一次刷新，[Refresh](../../server/refresh.ts) 负责节流和任务去重 | 当前检查到的持续刷新触发依赖页面；关闭页面后持续采集需要独立服务端调度 |

## 范围与依赖

先确定刷新调度契约；是 [REMOTE-01](remote-access.md) 实时验收和 [SYNC-01](private-sync.md) 的前置能力。

共同目标、数据边界和阶段安排见[总体统筹](README.md)。

## 待办与验收

### COLLECT-01：持续采集

- [ ] 将周期刷新责任落到受管理的后端服务，复用 Refresh 去重和本地/账户间隔配置；前端负责查询状态和显式手动刷新。
- [ ] 明确自动刷新关闭语义、配置变更生效、启动与停止清理、账户两种能力的独立失败、网络重试与退避。
- [ ] 处理睡眠唤醒、服务重启和长时间无人打开页面；启动注册不等于进程持续在线。
- [ ] 明确数据新鲜度目标：正常在线情况下，本地数据应在配置采集间隔加一次扫描耗时内可见；同步延迟另算。分别暴露最后成功采集、失败与进行中状态。

#### 验收标准

关闭所有网页后，用合成源追加记录，跨多个采集周期仍能从 API 查到变化；重开页面显示正确更新时间；账户失败不阻断本地采集，退出不遗留定时任务。

## 待决策问题

自动刷新关闭、配置变更、睡眠恢复和失败退避的具体语义。

## 完成记录

尚无完成记录。完成时记录工作项编号、实现文件或 PR、验证日期、实际结果及剩余限制，再勾选对应任务并更新总览状态。
