# 真实数据与在线示例共用前端：验收记录

日期：2026-09-08。

## 交付

- 本地工作台：[127.0.0.1:8765](http://127.0.0.1:8765/)。从仓库开发进程切换为现有受管理的生产启动，使用原数据库；设置序列化 SHA-256 前后一致，开机启动保持关闭。服务身份校验通过，返回 HTML 与当前生产构建一致。
- 在线示例：[原 Cloudflare 域名](https://codex-usage-showcase.sorenliu.workers.dev)。运行同一 React 前端，在浏览器内查询确定性合成 SQLite 数据，替换旧独立展示页面。
- 本地账户额度曾短暂超时，后续刷新已恢复：最终检查时额度及每日历史均无错误，身份已确认，额度快照非陈旧。本机记录持续独立更新。

## 构建与接口

正式 API、统计口径及 URL 查询语义保持不变。查询引擎仅将依赖收窄为查询／设置接口，数值辅助方法移至无 Node 运行时依赖的模块。浏览器与原生 SQLite 共用 SQL 和成本计算。

示例模式集中替换数据传输和时钟，保留筛选、时间下钻、任务与轮次、团队、设置、参考价格等真实页面。设置按浏览器 origin 保存；本机专属操作不可用。示例时间固定为 2026-09-08T18:00:00Z。

示例构建模块检查排除导入器、账户读取器、数据库文件操作和服务控制代码；正式前端检查排除示例模块与 sql.js。公开构建仅包含前端、合成 fixture、WASM 及许可文件。构建不请求任何个人实例。

## 验证结果

- 类型检查通过；现有与新增测试共 46/46 通过。
- 浏览器／原生 SQLite 差分：三个时区、时间边界、组合筛选、分组、分页排序、多模型轮次、团队汇总、参考成本、缺失值及超安全整数范围数据均通过。
- 设置持久化／隔离、非法设置、刷新、不支持的操作及未知任务测试通过。
- 生产构建、示例构建、smoke、安装包 smoke 和部署 dry-run 通过。
- 公开 14 个资源均与本地 SHA-256 一致；总览、分析、任务列表、完整任务、设置五个深链接通过 HTTP 验证。
- 两种模式均在真实可见浏览器检查 1440、1280、1024、768、390px。总览、分析、任务、完整任务／团队和设置均无页面横向溢出。原始测量见 [responsive-checks.json](responsive-checks.json)。
- 示例浏览器操作验证：今天为 18.5M 并使用小时粒度；09-05 下钻为 31.4M；轮次展开、子 agent 导航、搜索空结果与清除、排序分页、成本开启与刷新后保留均正常。
- 本地真实页面已验证数据加载、任务展开、团队区域、设置及各档宽度；不保存真实数据页面截图。

线上版本：`3617834d-b468-4f2f-beba-47ffcc67704a`。本轮实施前版本：`9db14b62-878f-4e8c-908e-fd85f115c66d`。现有域名与 Worker 名称保持不变，未新增云数据库。

构建保留原有主 JavaScript 分块大于 500 kB 的提示。示例另有约 119.55 kB 的数据模块及 658.41 kB 的 SQLite WASM，均仅在示例模式加载；WASM gzip 约 322.99 kB。正式应用不承担这部分大小。

## 合成数据截图与复现

截图取自已发布的在线示例，见 [桌面总览](overview-desktop.png)、[窄屏总览](overview-mobile.png)及[任务团队](team-desktop.png)。

构建、预览、发布与回退步骤见 [示例站说明](../../../showcase/README.md)。后续 README 与公开截图更新随代码一并提交；未发布新的安装包版本。
