# ATLAS 交互动效：实现与验收

日期：2026-09-09。依据本轮已批准的交互动效方案实现；当前规范以本文为准，09-08 文档保留为历史记录。

## 设计语言

沿用冷灰背景、白色表面、群青强调色和准确直出的数字。时间与粒度使用同一个白色选中块，主导航和分析维度使用群青下划线。浮层从触发器边缘出现；折叠释放自身空间；层级进入与返回具有方向。主导航页面本身直接切换。

| 动效 | 时长 | 方式 |
| --- | --- | --- |
| 按下 / 悬停 | 80 / 140ms | 表面内阴影和颜色，点击区域原位 |
| 局部结果、图标 | 160ms | 透明度 |
| 浮层进入 / 退出 | 200 / 140ms | 透明度和最多 6px 位移；向上弹出反向 |
| 小型折叠、开关、时区区域 | 220ms | 高度、滑块位置 |
| 范围标记、普通面板、趋势布局 | 260ms | 位移、宽度、高度 |
| 层级进入 / 返回 | 240ms | 桌面 8px，900px 及以下 16px，反向返回 |
| 可比较图形 | 320ms | Recharts 插值，ease-out |

位置及展开使用 `cubic-bezier(0.22, 1, 0.36, 1)`；浮层退出使用 `cubic-bezier(0.4, 0, 1, 1)`。无启动延迟。减少动态效果偏好启用时，取消 CSS 动画和过渡、结束运行中的 WAAPI 动画，并关闭图形插值。

## 全部交互评估与实现映射

| 场景 | 判断与原因 | 实现 |
| --- | --- | --- |
| 时间范围、日 / 小时 | 重点；表达明确的选中位置和目标宽度 | `Segmented`、`useActiveRule`，首次、字体完成及缩放直接测量对齐 |
| 主导航、分析维度 | 重点；保持连续的位置线索 | 共用测量下划线，页面直接切换 |
| 普通按钮、图标按钮、搜索、取消 | 轻量；立即确认操作，不移动命中区域 | `motion.css` 的颜色和内阴影 |
| 返回、完整任务、外部链接 | 轻量；方向图标提示导航关系 | 颜色过渡，已有方向图标最多 2px |
| 筛选、时间菜单、范围说明 | 重点；表达浮层归属，完整退出 | `MotionDetails` popup；嵌套 portal 归属、Escape、焦点恢复 |
| 模型、项目、排序、时区、来源选择 | 重点；表达实际弹出位置 | `Choice` + `usePresence`，按剩余空间选择上下方向 |
| 自定义时间 | 重点；分开编辑状态和生效范围 | `Collapse`；打开不改变选中值，校验成功才提交，取消恢复触发器焦点 |
| 筛选标签 | 轻量；移除及剩余标签归位 | `PresenceList` 保留退出项，透明度和位置过渡 |
| 目录选中、同层对象切换 | 轻量；目录位置稳定，详情表示更新 | 选中颜色 / 边线、局部结果淡入 |
| 目录到任务、完整任务、子 agent、返回 | 重点；表达层级 | `MotionNavigation`、`useHierarchyMotion`，限定到详情容器 |
| Turn 展开 | 重点；保持原表格行与按钮 | `ExpandingRow` 在有效 `tr / td` 内动画，箭头 90° |
| 趋势展开 / 收起 | 重点；空间变化和摘要布局对应 | `useTrendLayout` 测量高度与摘要位置，动画期间固定图形宽度 |
| 比较、Agent、关联任务、官方每日记录、来源、访问说明 | 重点；统一双向折叠 | `MotionDetails`，关闭立即 inert，结束后卸载 |
| 搜索、清空、低缓存、排序、分页 | 轻量；控制立即响应，结果确认更新 | 前端操作身份 + 局部 tbody / 结果淡入，表头及工具栏原位 |
| 图表悬停、键盘定位 | 轻量；读数和定位及时 | Tooltip 关闭位置缓动，内容使用当前数据 |
| 图表日期下钻、返回 | 轻量；时间轴不再可比较 | 新图形局部淡入 |
| Brush 拖动 / 键盘调整 | 原生；保持跟手 | 保留 Recharts 行为与现有范围提交，不给拖动位置加缓动 |
| 柱图、面积图数据更新 | 条件重点；只连接可比较数据 | `ChartMotion` 检查系列、桶身份及顺序、有限值、实际绘图区和坐标映射 |
| 额度条 | 轻量；同额度窗口的用户更新才过渡 | `QuotaProgress`，百分比即时呈现 |
| 启动开关、保存 | 轻量；反馈必须对应真实结果 | busy / disabled 即时，确认后更新，失败保留原状态 |
| 时区模式 | 重点；高度变化有连续性 | `AdaptiveRegion`，保留业务输入组件 |
| 模型价格、长上下文 | 重点；表单增删和空间对应 | 分组折叠、稳定 UUID 前端身份、新增聚焦、删除后转移焦点、恢复预置淡入 |
| 复制 | 轻量；只在成功后确认 | `useCopyFeedback`，2 秒恢复、重复操作重计时、失败文字、卸载后作废 |
| 加载 / 错误 / 空状态 | 轻量；读数状态明确 | 既有状态区域，保留旧结果时显示“正在更新…” |
| 数字、静态说明、输入字符、普通数据行、滚动 | 静态；保证精确性和阅读连续性 | 不滚动计数、不逐行入场、不拦截原生输入或滚动 |

## 状态与生命周期

- `motion-state.ts` 记录查询操作和按来源手动刷新身份。每个结果只消费一次；相同载荷和后台轮询不重复播放。缓存结果的时间戳可以早于点击，仍按本次用户选择处理。
- `motion-data.ts` 从查询结果派生动画状态，不改公共 API、URL 契约或设置持久化结构。新查询使用同路由旧结果占位时明确显示更新中；错误消费操作身份，避免后续轮询误播。
- `animateMotion` 每个元素最多一个受管动画。新动作取消旧动作，旧完成回调无法清除新动画；组件卸载清理受管动画。
- 折叠从当前可见高度反向，内容异步改变高度时重新测量内层，窗口变化结束临时动画。关闭立即 inert / aria-hidden，退出中的表单 fieldset disabled。
- Recharts 实际图形通过 portal 渲染；淡入定位到真实 `.recharts-area` / `.recharts-bar` 图层，坐标轴和工具栏不参与淡入。
- 没有增加动画依赖。Vite 开发代理同时修正为 `^/api(?:/|$)`，防止 `/api.ts` 前端模块被误代理；示例模式不连接本地 API。

## 自定义收起闪烁回归

用户报告“从自定义切换回其他按钮时，界面会闪烁一下”。在 390px 宽度，用 CUA 浏览器中逐帧 DOM 几何采样复现。

**原因**：WAAPI 完成后同步释放 `fill`，React 的退出卸载发生在下一次提交；中间一帧重新呈现了表单自然高度，导致下方内容回跳。

| 采样 | 退出末段高度（px） | 回弹 |
| --- | --- | --- |
| 修复前 | `0.039 → 0.008 → 0 → 233 → 0` | 1 次，下方趋势同步回跳 233px |
| 修复后 | `0.039 → 0.008 → 0` | 0 次，无退出节点残留 |

修复在共用 `usePresence`：关闭完成时同步保留 `height: 0`（非 popup）和 `opacity: 0`，直到 React 卸载；再次打开完成后释放临时样式。这同时覆盖自定义、轮次详情及共用折叠。

## 验收证据与范围

环境：Windows，Codex 内置 CUA 浏览器，Vite showcase `127.0.0.1:5181`，固定 2026-09-08 合成数据。使用语义 / AX 操作与 CDP 开发测试能力读取 DOM、实际动画及几何。没有使用独立 Playwright 脚本。

| 验收 | 结果 |
| --- | --- |
| 范围切换 | 今天 18.5M、30 天 119.21M；结束后标记与真实选中按钮几何一致 |
| 自定义编辑 / 取消 / 校验 / 应用 | 编辑保留原范围；反向日期显示校验信息且 URL 不变；有效日期应用后才选中自定义并回焦触发器 |
| 自定义退出闪烁 | 已复现并修复；逐帧回弹 0 次，结束后退出容器数 0 |
| 嵌套 Escape | 第一层关闭 Choice、父筛选仍打开、焦点回模型按钮；第二次关闭父层、焦点回筛选 summary |
| 浮层方向 / 反向 | 实测下弹 `-6px`、上弹 `+6px`；关闭读取当前透明度和位移，140ms；退出立即 inert |
| Turn 开合 | 详情高度 0 ↔ 221px，260ms；收起后原 `turn-03` 按钮仍获得焦点，无退出行残留 |
| 趋势和层级 | 记录到 260ms 趋势高度 / 摘要位移动画，目录进入任务记录到 240ms / 16px 详情进入 |
| 图表淡入 | 实际 Recharts 面积图层上捕获 `atlas:chart-fade`，160ms |
| 图表插值 | 隔离适配器只给原时间桶增加 10,000 Token，保持刻度；手动刷新记录到 `morph` 开始和结束 |
| 数据触发分类 | 自动测试覆盖首次、缓存命中、后台变更、相同数据、不兼容坐标、减少动态效果 |
| 模型表单 | 新增第 5 项聚焦 ID；删除第 2 项后剩余 UUID 和第 5 项输入 `qa-model` 保持，焦点转相邻项；恢复预置正常 |
| 时区模式 | 记录到实际控制区域 40 → 68.7px、220ms 高度变化 |
| 保存 | 模拟 700ms 延迟时 busy=true 且 disabled；成功显示“设置已保存”；模拟失败原位显示原因并恢复可操作 |
| 复制 | 成功显示“已复制”，2 秒后为空；模拟剪贴板拒绝时显示手动复制提示 |
| 启动开关 | 隔离内存接口：等待时 busy=true 且值不提前变化；成功后 false → true；下一次失败保留 true 并显示原因 |
| 响应式 | 390 / 900 / 1440px 下检查总览、分析或设置，文档 scrollWidth-clientWidth=0；390px 分析图形宽度 309px |
| 减少动态效果 | 实时启用偏好后自定义正常展开、控件可交互、`document.getAnimations().length=0`，标记 transition=0s |
| 工程 | `npm run build`（含类型检查）、`npm run showcase:build`、`npm test`；65 项测试，62 通过、3 平台跳过 |

测试适配器和动画采样器仅在浏览器内存中注入，刷新页面后全部移除；没有调用真实系统启动设置或账户服务。响应式验证为桌面浏览器视口模拟，不是实体手机或所有浏览器的兼容性认证。生产构建仍有 Vite 单块超过 500kB 的体积提示，不影响构建通过。

新测试集中于结果触发分类、图形可比较条件、手动刷新取消竞争、动画中断与卸载后的旧完成回调，不逐条复制 CSS。自定义退出终点使用真实浏览器逐帧回归验证。

## 后续体验修正：统计切换与图表提示

用户进一步反馈，切换时间口径时文字和统计区域闪烁、图表提示浮窗没有可见动画。本次调整覆盖以下行为，并优先于上表中的摘要淡入描述：

- 总览统计摘要关闭整体淡入，读数直接更新，更新中状态仍保留。浏览器逐帧采样统计容器透明度始终为 1。
- 已有数据的不可比较图形切换，透明度由 0.7 恢复到 1，不再从完全透明开始；首次图形仍可淡入。实际范围切换采样的图形最低透明度为 0.7。
- Recharts 会先挂载隐藏的 Tooltip 来测量尺寸，原先内容挂载即播放的 CSS 动画可能在显示前结束。现将启动条件改成 Tooltip wrapper 实际 `visibility: visible`，总览和分析页共用 160ms 淡入。
- 悬停验收捕获 `atlas-tooltip-enter`，触发时 wrapper 为 visible、时长 0.16s；沿曲线移动到下一数据点，事件计数保持 1，内容准确更新，位置 transition 为 0s。
