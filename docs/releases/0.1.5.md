# 0.1.5

- Add interruptible selection markers, bidirectional disclosures, hierarchy transitions, trend layout motion, comparable chart interpolation and truthful form feedback.
- Fix custom-range collapse end-frame rebound and statistics fading out when switching ranges. Start chart tooltip entry only when actually visible.
- Move seven bundled browser libraries to build dependencies. The isolated 0.1.4 comparison reduced installed files from 96.29 MiB to 25.15 MiB; versions and runtime functionality remain unchanged.
- Retain exact-tree Windows/macOS and Node 22/24/26 validation, installed-package smoke, OIDC publication and registry integrity verification.

The one-shot `Finish authorized 0.1.5 release` workflow continues the explicitly authorized release in GitHub after this computer shuts down. It stops on failed checks or unexpected branch changes. Its write permissions are scoped to the release job. The npm workflow also accepts manual dispatch on a tag, retaining all existing release gates.
