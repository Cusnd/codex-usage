# 0.1.6

- Keep day/hour and date-range area curves continuous, including dense-to-sparse transitions and mid-animation reversals. Use a 297ms nonlinear transition and smooth tooltip movement.
- Fix cold-switch jumps and entrance flicker. Identical refetches and query-status updates no longer cancel or restart an active chart transition.
- Prepare curve geometry once per target instead of sorting and sampling every animation frame. Keep the last successful chart while a new request is pending or fails.
- Validate three cold-start rounds in the production showcase: 12 complete transitions, frame-interval P95 of 9–13.7ms, maximum 23.1ms, no missing curves or long tasks. Also verify delayed responses, failures, stale results, reversal and reduced motion.
- Retain exact-tree platform checks, installed-package smoke, OIDC publishing and npm registry integrity verification.

The one-shot `Finish authorized 0.1.6 release` workflow completes the explicitly authorized integration and publication in GitHub after the local computer shuts down. It stops on failed checks or unexpected branch changes. The separate general-purpose release automation TODO is not included in this release.
