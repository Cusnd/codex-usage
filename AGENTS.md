# 项目指引

## 工具环境

本地 Mamba 环境 `codex-tools` 提供额外的 Python 工具。按需使用解释器 `C:\Users\liuso\miniforge3\envs\codex-tools\python.exe`，可以自行在该环境中安装需要的依赖。

进行浏览器验收测试时，使用 browser-control 技能，不使用 Playwright。

## 工作流入口

涉及代码修改任务的分支选择，以及 commit、PR、合并、版本标签或 npm 发布时，先阅读 [项目工作流](.agents/workflow.md)。同一任务中已读取且内容未变化时，无需重复读取。纯讨论和只读分析不需要加载该流程。
