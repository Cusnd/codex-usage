# 用量图录设计与功能验证

final result: passed

本报告评价已认可图录方向及随后增加的缓存／成本要求，不宣称与早期概念图逐像素相同。概念图和最终 Session 截图已在同一次视觉比较中查看；项目、全局 Turn 与窄窗口截图分别复核。最终导出使用同一宽度的桌面应用和一致示例数据，增加高度以呈现新增统计行。

## 初查问题与修复

- P2：顶部留白和趋势区域偏高，目录进入视野过晚。收紧标题、范围说明、图表控件和统计分项的垂直间距；保留趋势收起操作。
- P2：旧 CSS 的选中按钮仍显示青绿。修正选择器优先级，统一群青。
- P2：长项目目录撑高整个工作区。桌面目录使用独立滚动区域，右侧明细与分页保持清楚；窄窗口改为逐层进入。
- P2：全局筛选改变后，选中分组可能覆盖新的全局条件。增加范围冲突提示，不展示错误对象的数据。
- P2：早期生成图的部分数值不一致。以隔离数据库和程序断言生成最终截图，保留概念图仅作风格参考。
- 截图工具的长图在高分屏出现缩放异常；采用浏览器原生截图并匹配设备像素密度，重新导出清晰图像。

## 五项视觉核对

1. **字体与文字层级**：Inter 数字、系统中文字体；标题、摘要、列表及辅助文字层级分明。长标题允许换行，完整 ID 可复制，目录元数据可查看完整提示。
2. **布局与间距**：水平导航、上方趋势、左侧目录、右侧明细符合选定方向。新增缓存统计行使页面比概念图更长，这是用户新增需求的明确差异；没有强行缩小正文来塞满首屏。
3. **颜色与状态**：矿物白、墨黑、群青及细分隔线一致；当前维度、选中目录和展开轮次可辨识。键盘焦点有可见边框。
4. **图像与图标**：界面使用真正的文本、表格、Recharts 图表和现有 SVG 图标库；没有把概念图作为界面背景或伪造交互。导出图像比例与清晰度已复核。
5. **内容与口径**：真实标题已接入；范围内／完整 Session、缓存包含关系、模型构成、缺失字段与参考成本均有明确标识。设计截图明确标为示例。

## 功能证据

- 在真实服务中核对项目 → Session → Turn，名称存在、轮次组成返回、Token 仍为字符串；真实设置默认成本关闭、四个价格预置存在。
- 在隔离设计环境中验证标题搜索、六轮任务、双模型展开、完整 Session、刷新后返回 URL、单日下钻、恢复原范围、无匹配任务提示。
- 设置界面勾选成本、保存成功，再进入全局 Turn，缓存写入和 USD 参考金额生效。
- 键盘调整范围后 URL 与查询共同更新，示例总量从 119.21M 变为 111.01M。
- 520px 宽度下没有页面横向溢出；选择任务时目录隐藏、详情显示，返回时相反；趋势收起可用。
- 浏览器检查期间没有控制台 error/warn。
- `npm run build`、`npx tsc --noEmit --noUnusedLocals` 和 11 项测试通过。测试覆盖导入去重、名称索引、范围聚合、多模型、跨任务轮次、成本精度和长上下文、写入缺失、夏令时日界线。

## 证据文件与限制

- `docs/design/atlas/project-desktop.png`
- `docs/design/atlas/session-desktop.png`
- `docs/design/atlas/day-desktop.png`
- `docs/design/atlas/turn-cost-desktop.png`
- `docs/design/atlas/mobile-detail.png`

Vite 提示单个包大于 500kB；构建成功，当前未为此重组依赖。目录和表格较长时需要滚动，用户可收起趋势。成本是当前配置价格下的 API 参考额，不是订阅账单。未模拟所有外部账户故障；沿用现有错误与上次成功数据保留逻辑。
